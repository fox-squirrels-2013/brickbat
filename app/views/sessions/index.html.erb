<%=link_to "Log in with your Twitter account!", "/auth/twitter" unless session[:user_id]%>
<%=User.find_by_id(session[:user_id]).username if session[:user_id]%>
<%=link_to "Log out", "/logout" if session[:user_id]%>